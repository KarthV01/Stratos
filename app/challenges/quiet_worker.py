BASELINE = {
    "commit_policy": "unconditional",
    "effect_policy": "increment",
    "resolve_recovered_pending": False,
}

# This intentionally begins with the same behavior as the broken baseline.
CANDIDATE = {
    "commit_policy": "unconditional",
    "effect_policy": "increment",
    "resolve_recovered_pending": False,
}
